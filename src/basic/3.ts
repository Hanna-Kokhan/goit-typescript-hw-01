type StringOrNumber = string | number;

let flexibleValue: StringOrNumber;

flexibleValue = "Hello";
flexibleValue = 123;

type Status = "enable" | "disable";

let status: Status;

status = "enable";
status = "disable";
